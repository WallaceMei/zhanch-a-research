# Optional Tushare EOD data source. Keep the actual token in local .env only.
TUSHARE_TOKEN=
TUSHARE_HTTP_URL=

# Optional QMT data service endpoint.
QMT_IP=127.0.0.1
QMT_PORT=58610

# Optional local warehouse root used by the warehouse adapter.
DW_ROOT=
