# Project1 Paper — 开跑前清单(2026-07-06 14:30 更新)

> 状态基线(2026-07-04 周六):Step2-4 代码全部搭完,mock 端到端演习通过,看板可开。
> **7-06(周一)盘中实测进展:③ MiniMax 已全关 ✅;①② 关掉大半,各剩一个只能盘前/早盘窗口验的点(见下)。**
> **护栏1 现状:剩余项通过 + Wallace 点头之前,真实 paper(state=real)不开跑。**

## 7-06 盘中已关闭的验收点 ✅

- **③ MiniMax 全关**:key 已配(用户级环境变量 MINIMAX_API_KEY;国内站 api.minimaxi.com + MiniMax-Text-01,默认配置零改动可用)。完整管线实测通过:真实调用→JSON→校验→留痕四件套(今天日期+SMOKE合成快照,只验管线合法过护栏2)。picks≤3/confidence 校验全过。
- **①(大半)**:今天真实 xtdata 1m 实测:首根=09:30竞价根确认(end标签成立);今早真实数据回放,增量监测器 vs 批量find_signal **10/10 对拍一致**(9只有信号,时刻全吻合);full_tick 可取。
- **②(大半)**:07-03 昨竞价缺口补数闸门实测:xtdata 1m首根 + tushare open 0.5%校验,**10/10 通过**,价/量/额数值合理。
- **①feed时序全关**:盘中12分钟实测(2只票,14:07-14:20):稳态每分钟每票恰好1根新完结bar,
  到达延迟 2.2~4.9s(轮询3s),标签连续无缺根无乱序,full_tick正常。
  → "抓那一刻"的真实时延 ≈ 信号bar收盘后 2~5s,可接受(记入成本/口径备注)。

---

## 剩余必过项(明早 2026-07-07 早盘窗口)

**①残留:早盘窗口内实测"抓信号那一刻"**(信号窗9:31-10:30 只在早盘存在):
| 时间 | 动作 | 命令(均在 research/project1_paper/ 下) |
|---|---|---|
| 09:25 | 连通复核 | `py -3.10 run_step2_live.py --check` |
| 09:29 | 2只票小样本实时监测 | `py -3.10 run_step2_live.py --codes 000001.SZ,600519.SH` |
| 10:35后 | 验收 | out/rt_live/ 产物:信号时刻 capture_price/tick价/时戳对得上;bars 与次日 canonical 对拍 |

**②残留:09:26 的 full_tick 竞价量准确性**(只能盘前验:9:25 后 volume=纯竞价量,盘中会变全天累计,7-06 盘中无法测):
- [ ] 明早 08:30 前:`py -3.10 update_daily_cache.py --asof 20260706`(补今天日线尾巴)
- [ ] 09:26:`py -3.10 live_pool.py --d8 20260707` 出池,检查 seed 数百/full_tick 有效率>90%/top12 结构合理
- [ ] 交叉验:选 2-3 只票的 full_tick 竞价量,次日与 canonical 09:30 首根 volume 对拍

**③ 已全关 ✅**(7-06 盘中,见上)

## ④ 全部通过后 → 正式开跑(需 Wallace 当次明确点头)

每日节奏(通过前全部用 state=drill 演练;通过后切 state=real):
```
盘后18:30  py -3.10 run_paper_settle.py --mode live --state real     (含日线尾巴更新)
盘前09:26  py -3.10 run_paper_morning.py --mode live --ai real --state real
盘中09:29  py -3.10 run_paper_intraday.py --mode live --state real --ack-unverified --live-verified
           (--live-verified 旗标 = ①实测通过+Wallace确认后才允许加)
看板       py -3.10 -m uvicorn app:app --host 127.0.0.1 --port 8766  (webapp/ 下,常开)
```
- [ ] 定时任务挂载(可选,跑顺几天后再自动化;先手动跑守着看)
- [ ] 第1周验收 = spec §7.1 管线验收(只验跑起来了没,不判AI价值)
- [ ] 4-8周攒 50-100 个进场决策后才做价值验收(spec §7.2:AI须同时赢全进+随机)

## 已知边界(诚实记录)

- 实时进场价口径 = 信号bar close(+0.08%滑点),与 Phase A 回测"下一根open"存在
  median 0bp / p90 23bp 的真实间隙(回放对拍已量化,留成本校准)。
- 三组对照公平 = 结构保证(同票同日单监测器单信号,跨组同价)。
- 全进组在 25%×3slot 上限下 ≠ Phase A 全进等权基座,数字不可对表。
- mock 演习(paper_state_drill/)结果无任何选股价值含义(机械规则占位,非AI)。
- 验收纪律(spec §7.3):不看绝对收益判AI / 单周赢不算 / 多策略警惕多重比较 / paper不能加速。
