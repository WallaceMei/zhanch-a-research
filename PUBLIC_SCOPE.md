# 公开快照范围

本仓库从本地研究工作区提取可公开的源码与文档，并刻意排除以下内容：

- 行情原始数据、分钟数据、CSV/Parquet 面板、回测逐笔结果和 HTML 数据分片。
- QMT、聚宽、Tushare 等服务的 token、`.env`、账户与本机运行状态。
- 日志、缓存、临时文件、压缩包及 Excel 工作簿。

`PUBLIC_SOURCE_MANIFEST.txt` 记录首次快照包含的文件清单。它是审计清单，不是运行时依赖。
